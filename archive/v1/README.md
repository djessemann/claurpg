# EMBERFALL — v1 (archival)

The original NROM (mapper 0) Dragon Quest-style RPG. Playable ROM:
`emberfall-v1.nes`. Source snapshot under `src/` and `tools/`. Superseded by
EREBUS (v2) at the repository root. See git tag `v1`.
